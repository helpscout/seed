{
  "name": "seed-floats",
  "version": "0.0.1",
  "description": "Floats utility pack for Seed",
  "main": "index.js",
  "homepage": "https://github.com/helpscout/seed-floats",
  "scripts": {
    "banner": "node ./scripts/banner.js",
    "build": "npm run build:main && npm run build:min && npm run banner",
    "build:main": "node-sass --include-path node_modules/seed-breakpoints/scss scss/pack/_seed-floats.scss dist/seed-floats.css",
    "build:min": "node-sass --include-path node_modules/seed-breakpoints/scss scss/pack/_seed-floats.scss dist/seed-floats.min.css --output-style compressed",
    "test": "node ./scripts/test.js && npm run build",
    "prepublish": "npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/helpscout/seed-floats.git"
  },
  "authors": [
    "ItsJonQ <itsjonq@gmail.com>"
  ],
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/helpscout/seed-floats/issues"
  },
  "engines": {
    "node": ">=0.10.1"
  },
  "devDependencies": {
    "node-sass": "^3.8.0"
  },
  "dependencies": {
    "seed-breakpoints": "0.0.2"
  }
}
